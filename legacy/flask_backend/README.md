# Archived Flask Backend

This directory preserves the former Flask, Flask-SocketIO, Python PTY, and
Pyright bridge implementation for historical reference only.

It is not installed, started, tested, or maintained by the active Bob IDE.
The supported server is `node-server/server.js`; Python business logic is
available through the official MCP service in `mcp_server.py`.
