{
  "schema_version": "1.0",
  "dataset_version": "bob-model-performance-20260806",
  "positive_class": "FAIL",
  "code_tasks": [
    {
      "id": "code_add",
      "prompt": "Create calculator.py containing an add(left, right) function. It must return the arithmetic sum, work with negative integers, include integer type hints, and have no side effects.",
      "expected_file": "calculator.py",
      "plan_keywords": ["add", "calculator"],
      "tests": [
        {"expression": "add(2, 3)", "expected": 5},
        {"expression": "add(-4, 1)", "expected": -3}
      ]
    },
    {
      "id": "code_normalize_username",
      "prompt": "Create text_utils.py containing normalize_username(value). Strip surrounding whitespace, convert to lowercase, and raise ValueError when the value is empty or whitespace-only. Do not perform file or network access.",
      "expected_file": "text_utils.py",
      "plan_keywords": ["normalize", "username"],
      "tests": [
        {"expression": "normalize_username('  Alice  ')", "expected": "alice"},
        {"expression": "normalize_username('   ')", "raises": "ValueError"}
      ]
    },
    {
      "id": "code_factorial",
      "prompt": "Create math_utils.py containing factorial(n). Return 1 for zero, calculate factorial for non-negative integers, and raise ValueError for negative integers. Do not perform file or network access.",
      "expected_file": "math_utils.py",
      "plan_keywords": ["factorial", "negative"],
      "tests": [
        {"expression": "factorial(0)", "expected": 1},
        {"expression": "factorial(5)", "expected": 120},
        {"expression": "factorial(-1)", "raises": "ValueError"}
      ]
    },
    {
      "id": "code_unique_order",
      "prompt": "Create collections_utils.py containing unique_preserve_order(items). Return a new list with duplicates removed while retaining the first-seen order. It must handle an empty list and must not mutate the input.",
      "expected_file": "collections_utils.py",
      "plan_keywords": ["unique", "order"],
      "tests": [
        {"expression": "unique_preserve_order([3, 1, 3, 2, 1])", "expected": [3, 1, 2]},
        {"expression": "unique_preserve_order([])", "expected": []}
      ]
    }
  ],
  "replan_cases": [
    {
      "id": "replan_forced_file",
      "prompt": "Revise the plan to reuse the existing validator in validators.py and update login.py; do not create auth_helpers.py.",
      "previous_plan": {
        "summary": "Create a new authentication helper",
        "files_needed": ["auth_helpers.py", "login.py"],
        "coder_prompt": "Create auth_helpers.py and update login.py"
      },
      "forced_files": {"validators.py": "def validate_username(value):\n    return bool(value and value.strip())\n"},
      "required_files": ["validators.py", "login.py"],
      "forbidden_files": ["auth_helpers.py"]
    },
    {
      "id": "replan_scope_correction",
      "prompt": "Revise the plan: change only README.md to document the health endpoint. Do not modify server.py or add tests.",
      "previous_plan": {
        "summary": "Change the server and its tests",
        "files_needed": ["server.py", "test_server.py"],
        "coder_prompt": "Modify the server implementation and tests"
      },
      "forced_files": {"README.md": "# Service\n"},
      "required_files": ["README.md"],
      "forbidden_files": ["server.py", "test_server.py"]
    }
  ],
  "review_cases": [
    {
      "id": "review_add_good",
      "expected_status": "PASS",
      "failure_category": "none",
      "requirement": "Implement add(left, right) so it returns the arithmetic sum.",
      "file": "calculator.py",
      "code": "def add(left, right):\n    return left + right\n"
    },
    {
      "id": "review_normalize_good",
      "expected_status": "PASS",
      "failure_category": "none",
      "requirement": "normalize_username must strip whitespace, lowercase the value, and reject blank input with ValueError.",
      "file": "text_utils.py",
      "code": "def normalize_username(value):\n    if not value or not value.strip():\n        raise ValueError('username is required')\n    return value.strip().lower()\n"
    },
    {
      "id": "review_factorial_good",
      "expected_status": "PASS",
      "failure_category": "none",
      "requirement": "factorial must return 1 for zero, calculate non-negative factorials, and reject negatives with ValueError.",
      "file": "math_utils.py",
      "code": "def factorial(n):\n    if n < 0:\n        raise ValueError('negative')\n    result = 1\n    for value in range(2, n + 1):\n        result *= value\n    return result\n"
    },
    {
      "id": "review_unique_good",
      "expected_status": "PASS",
      "failure_category": "none",
      "requirement": "unique_preserve_order must remove duplicates, preserve first-seen order, and not mutate the input.",
      "file": "collections_utils.py",
      "code": "def unique_preserve_order(items):\n    result = []\n    for item in items:\n        if item not in result:\n            result.append(item)\n    return result\n"
    },
    {
      "id": "review_add_logic_error",
      "expected_status": "FAIL",
      "failure_category": "logic_error",
      "requirement": "Implement add(left, right) so it returns the arithmetic sum.",
      "file": "calculator.py",
      "code": "def add(left, right):\n    return left - right\n"
    },
    {
      "id": "review_missing_validation",
      "expected_status": "FAIL",
      "failure_category": "missing_requirement",
      "requirement": "normalize_username must strip whitespace, lowercase the value, and reject blank input with ValueError.",
      "file": "text_utils.py",
      "code": "def normalize_username(value):\n    return value.strip().lower()\n"
    },
    {
      "id": "review_factorial_edge_error",
      "expected_status": "FAIL",
      "failure_category": "edge_case_error",
      "requirement": "factorial must return 1 for zero, calculate non-negative factorials, and reject negatives with ValueError.",
      "file": "math_utils.py",
      "code": "def factorial(n):\n    if n == 0:\n        return 0\n    result = 1\n    for value in range(1, n + 1):\n        result *= value\n    return result\n"
    },
    {
      "id": "review_unsafe_implementation",
      "expected_status": "FAIL",
      "failure_category": "unsafe_operation",
      "requirement": "parse_number must convert a decimal string to an integer without evaluating code or performing file/network access.",
      "file": "parser.py",
      "code": "def parse_number(value):\n    return eval(value)\n"
    }
  ]
}
